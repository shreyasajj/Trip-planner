# Minimal Mosquitto config for the trip companion.
# WARNING: anonymous access is fine on a trusted home LAN, but if you expose
# this broker to the internet, set up password_file + TLS instead.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
